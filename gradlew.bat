@rem GlassPhotos gradlew launcher stub.
@rem CI uses gradle/actions/setup-gradle which fetches the wrapper jar automatically
@rem from gradle-wrapper.properties, so this file is a placeholder for local Windows use —
@rem run `gradle wrapper` once locally (or let CI generate it) to populate gradle-wrapper.jar.
@echo off
gradle %*
